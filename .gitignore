# Secrets + per-node config
.env
config.yaml

# Rendered deploy tree + state (generated by `spark-lab apply`)
deploy/
.sparklab-state/

# Managed Python venv
.venv/
__pycache__/
*.pyc

# OS / editor noise
.DS_Store
*.swp
