# Secrets + per-node config
.env
config.yaml

# Rendered deploy tree + state (generated by `spark-lab apply`)
deploy/
.sparklab-state/

# Generated recipe candidates (`spark-lab recipes convert`) -- review + promote manually
recipes/candidates/

# Staging E2E + rollback report (generated by scripts/staging.sh)
staging-report/

# Managed Python venv
.venv/
.venv-test/
__pycache__/
*.pyc

# Test artifacts
.coverage
coverage.xml

# Build / packaging artifacts (pip install -e ., wheel builds)
build/
dist/
*.egg-info/

# OS / editor noise
.DS_Store
*.swp
