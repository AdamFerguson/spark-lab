# Generated by spark-lab from config.yaml (litellm.extra_models) -- do not edit by hand.
# Externally-run models the gateway serves but spark-lab does NOT launch or stop
# (e.g. a server started by hand or by a kit's own script). Merged alongside the
# generated model_config.yaml via the gateway `include:`. Edit the source list in
# config.yaml (litellm.extra_models), not this file.
{{ extra_models_yaml }}
