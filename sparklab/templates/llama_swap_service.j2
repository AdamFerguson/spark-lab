# Generated by spark-lab from config.yaml -- do not edit by hand.
# Installed to ~/.config/systemd/user/llama-swap.service by
# `spark-lab zoo prepare` (which also enables lingering so the zoo survives
# reboots / sits ready at boot).
[Unit]
Description=llama-swap model swapper (spark-lab zoo)

[Service]
ExecStart={{ swap_unit_execstart }}
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
